[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hermes-9router-tts"
version = "0.1.0"
description = "Text-to-speech via 9Router gateway with Elevenlabs direct multi-key rotation for Hermes Agent"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "requests",
]

[project.urls]
Homepage = "https://github.com/dhansxd/hermes-9router-tts-plugin"
